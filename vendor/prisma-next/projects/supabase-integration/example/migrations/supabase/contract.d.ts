// Auto-generated mirror of the @prisma-next/extension-supabase contract.
// Regenerated by `prisma-next install` (or equivalent). Do not edit by hand.

import type { TypedContract } from '@prisma-next/contract-core';

export interface Contract
  extends TypedContract<{
    spaceId: 'supabase';
    namespaces: ['auth', 'storage'];
    defaultControlPolicy: 'external';
    models: {
      AuthUser: {
        namespace: 'auth';
        tableName: 'users';
        fields: {
          id: { type: 'uuid'; primary: true };
          email: { type: 'text'; nullable: true };
          phone: { type: 'text'; nullable: true };
          emailConfirmedAt: { type: 'timestamptz'; nullable: true };
          createdAt: { type: 'timestamptz' };
          updatedAt: { type: 'timestamptz' };
        };
      };
      StorageBucket: {
        namespace: 'storage';
        tableName: 'buckets';
        fields: {
          id: { type: 'text'; primary: true };
          name: { type: 'text' };
          public: { type: 'boolean' };
          createdAt: { type: 'timestamptz' };
          updatedAt: { type: 'timestamptz' };
        };
      };
    };
    // Functions are not contract elements in v0.1 (see decisions.md C4).
    // auth.uid() / auth.jwt() / auth.role() are referenced as opaque strings
    // inside RLS predicates; column-default invocations go through the
    // framework's DefaultFunctionRegistry.
  }> {}
