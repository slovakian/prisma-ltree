# Brief: S3-D3 — narrow the Form 2 grep-gate pattern (CodeRabbit review response)

## Task

CodeRabbit finding on `drive/calibration/grep-library.md` (Contract-cast hygiene,
Form 2), accepted as valid: `rg -U 'blindCast<[^(]*\(\s*contractJson\s*\)' packages/`
matches ANY `blindCast<T>(contractJson)` — including the API-boundary casts the
section's own carve-out note declares out of scope — a future false-positive trap.
Narrow Form 2 so it only fires on `contractJson` values *derived from a
`contractSpace`*: use a multi-line pattern that ties the cast to a preceding
`contractSpace`-derived assignment of the picked variable within the same region
(e.g. match `contractSpace` … newline(s) … `blindCast<…>(contractJson)` within a
bounded window via `rg -U`), or an equivalently precise two-step recipe (find bare
Form 2 hits, then confirm the picked variable's assignment reads `contractSpace`) —
your choice, documented in the entry. Requirements:

1. Positive control still holds: the pattern (or recipe) catches the historical
   control-stack cast at commit `d9cb3a9ce~1`.
2. No false positive on a synthetic API-boundary shape (`const contractJson =
   options.contractJson; blindCast<Contract>(contractJson)` with no `contractSpace`
   nearby) — demonstrate in your report, not in the doc.
3. Repo-wide run passes today (exit 1).
4. The carve-out comment updated to match the narrowed semantics.

Doc-only change; no code.

## Scope

**In:** `drive/calibration/grep-library.md` (the Form 2 lines + adjacent comment).
**Out:** everything else. No replies to GitHub threads.

## Completed when

- [ ] Narrowed Form 2 documented; three verifications from the Task run and reported
      (positive control, synthetic negative, repo-wide pass).
- [ ] Gate: `pnpm --filter @prisma-next/framework-components lint` not needed —
      markdown only; just verify the greps.

## Operational metadata

- **Time-box:** 20 min. Halt condition: no pattern/recipe satisfies all three
  verifications — report the best trade-off instead of shipping a lie.
