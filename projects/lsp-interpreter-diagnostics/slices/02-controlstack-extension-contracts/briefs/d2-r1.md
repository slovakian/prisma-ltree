# Brief: S2-D2 — CLI emit simplification + grep gate

## Task

In `packages/1-framework/3-tooling/cli/src/control-api/operations/contract-emit.ts`,
collapse the `ContractSourceContext` construction to pure property picks off the
control stack: `composedExtensionContracts` becomes `stack.extensionContracts`
(landed in S2-D1); the inline `toExtensionInputs(...)` call, the map-building block,
and **both** `blindCast`s in that block are deleted. `composedExtensionPacks` stays
derived from `stack.extensionPacks.map((p) => p.id)` (S2-D1 pinned that
`extensionIds` is NOT equivalent — it prepends family/target/adapter ids; do not use
it). If the `toExtensionInputs` import becomes unused in this file, remove the import;
the helper itself and its other consumers stay untouched. Emit behavior must be
bit-identical.

Then document the grep gate in `drive/calibration/grep-library.md` (follow the file's
existing entry format): **no `contractJson` casts outside framework-components** —
e.g. `rg "blindCast<Contract" packages/ --glob '!**/1-core/framework-components/**'`
plus a line on why (the ControlStack owns the one sanctioned narrowing; consumers
property-pick). Verify the gate passes on your diff.

Tests: the emit path is covered by existing CLI/e2e/fixtures suites — this dispatch
adds no new behavior, so no new tests are expected; the gate is the fixture/e2e
stability itself.

## Scope

**In:** `contract-emit.ts` (the context-assembly block + imports only),
`drive/calibration/grep-library.md`.
**Out:** `toExtensionInputs` and its module; other CLI operations; anything under
`packages/1-framework/1-core/`, `packages/2-*`; no `projects/**` writes.

## Completed when

- [ ] `contract-emit.ts` context construction contains zero casts and no
      `toExtensionInputs` call; `composedExtensionContracts` = `stack.extensionContracts`.
- [ ] Grep gate documented in `drive/calibration/grep-library.md` and passing
      repo-wide.
- [ ] Gates green: `pnpm --filter @prisma-next/cli test` + package typecheck + lint,
      `pnpm typecheck`, `pnpm test:e2e`, `pnpm fixtures:check`, `pnpm lint:deps`.
      Net cast-ratchet for the slice: −1 (D1 +1, D2 −2).

## Standing instruction

Stay focused on the goal; control scope.

## References

(Resumed — you know the paths.) S2-D1 landed `stack.extensionContracts`
(`8e5eadd75`); reviewer confirmed old-assembly semantics match by reference.

## Operational metadata

- **Time-box:** 45 min. Halt conditions: emit fixtures differ (bit-level) after the
  swap — that falsifies the behavior-preserving claim, surface immediately with the
  diff; the CLI file resists a cast-free pick for any context property (would mean
  the stack is still missing a fact — surface, don't re-derive inline).
